namespace Airhockey.Events {
    public enum GameSignal {
        OnGoalScored,
        OnRoundEnd,
        OnMatchEnd,
        OnSlowmoStart,
        OnSlowmoEnd,
        OnStartSpawnPortals,
        OnStopSpawnPortals
    }
}